name: Fiyat Güncelle

on:
  schedule:
    - cron: '0 2 * * *'  # Her gün 05:00 TR saati
  workflow_dispatch:       # Manuel çalıştırma butonu

jobs:
  update-prices:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Repoyu İndir
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Kütüphane Yükle
        run: pip install requests

      - name: Fiyatları Güncelle
        env:
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
        run: python update_prices.py

      - name: prices.json Kaydet
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "PC Ustası Bot"
          git add prices.json
          git diff --staged --quiet || git commit -m "💰 Fiyatlar güncellendi - $(date +'%d.%m.%Y %H:%M')"
          git push
